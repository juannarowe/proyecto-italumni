// src/features/events/types/event.types.ts

/**
 * Main interface for an Event
 */
export interface Event {
    id: number;
    name: string;
    location: string;
    date: string;
    description: string;
}
